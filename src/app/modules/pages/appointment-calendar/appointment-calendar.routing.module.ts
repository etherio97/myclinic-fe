import { Route } from '@angular/router';
import { AppointmentCalendarComponent } from './appointment-calendar.component';

export const appointmentCalendarRoutes: Route[] = [
    {
        path: '',
        component: AppointmentCalendarComponent,
    },
];
